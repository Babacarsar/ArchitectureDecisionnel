checks for DimVendors:
  - schema:
      warn:
        when required column missing:
          - vendorid
          - vendorname
      fail:
        when forbidden column present:
          - column_name1
          - column_name2
 